package science;import java.io.File;import java.io.IOException;import java.util.Scanner;public class Science {		public static final int CONTROL_GROUP = 0;	public static final int HUFFMAN_TREE = 1;	public static final int BINARY_TREE = 2;	public static Scanner scan = new Scanner(System.in);	public static File InFile;	public static File OutFile;	public static int runnum;		/**	 * @param args	 * @throws IOException 	 */	public static void main(String[] args) throws IOException {		// TODO Auto-generated method stub		int t = getType();		InFile = new File(args[0]);		OutFile = new File(args[1]);				if (t == Science.CONTROL_GROUP) {			long timeC = Control.Run();			System.out.println("Time For Control: " + timeC);		}		else if (t == Science.BINARY_TREE) {						System.out.println("");		}		else if (t == Science.HUFFMAN_TREE) {					}					}		public static int getType() {		int a;		int b;				do {		System.out.println("Enter 0 for Control, 1 for Huffman Tree, \nand 2 For Standard Binary Tree");			System.out.print(">> ");		a = scan.nextInt();		}		while (a != Science.CONTROL_GROUP ^ a != Science.HUFFMAN_TREE ^ a != Science.BINARY_TREE);				do {			System.out.print("Number Of Times To Run >> ");			b = scan.nextInt();		}		while(b < 1);		runnum = b;		return a;	}}